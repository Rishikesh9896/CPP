#include <iostream>
using namespace std;

int main(){
   /* int a;
    cin>>a;
    cout << "Value of a is " << a << endl;

    if(a>0){
        cout << " A is positive " << endl;
    }
    else{
        cout << "A is negative" << endl;
    }
    */

    //int a , b;
   // cin >> a >> b;
   // cout << "Value of a and b is " << a << " " << b << endl;

   // int r;
   // r = cin.get();
   // cout<< "Value of r is " << r << endl;

  /* int a , b;
   cout<< "Enter the value of a " << endl;
   cin >> a;
   cout << "Enter the value of b " << endl;
   cin >> b;

   if(a>b){
    cout << "A is greater" << endl;
   }

   if(b>a){
    cout<< "B is greater " << endl;
   }
   */
   
   /*
   int a;
   cout<< "Enter the value of a " << endl;
   cin >> a;

   if(a > 0) {
    cout<< " A is positive";
   }

   else{                       // we can also use else if block too
    if(a < 0){
        cout<< "A is negative";
    }
    else{
        cout<< " A is 0";
    }
   }
   */

   // HOME WORK

   char ch;
   cout << "Enter the value of the character " << endl;
   cin >> ch;

   if(ch>= 'a' && ch<='z'){
    cout << "It is lower case";
   }

   else if(ch >='A' && ch <='Z'){
    cout << " It is upper case";
   }

   else if(ch>='0' && ch<='9'){
    cout << "It is numeric";
   }

    









}